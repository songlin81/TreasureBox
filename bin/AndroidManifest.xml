<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linstudio.treasurebox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"	
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light" >
        <!-- 应用列表中应用名称在AndroidManifest.xml文件中application标签中的android:label="@string/app_name"设置。 -->
        <!-- 手机桌面上图标下面的名称在AndroidManifest.xml文件中默认activity中的android:label=@string/app_name设置。 -->
        <activity android:name="com.linstudio.treasurebox.SplashActivity" android:label="@string/app_name" android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen">
			<intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.linstudio.treasurebox.LandingActivity"
            android:label="@string/app_name" >
        </activity>
        
    </application>

</manifest>
